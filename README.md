# AI Debate Arena

MERN monorepo with a Next.js 16 client and an Express/MongoDB API.

## Development

Install dependencies with `npm install`, then run `npm run dev:client` and
`npm run dev:server` in separate terminals.
